{
  "name": "voxora-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix backend --production=false && npm install --prefix frontend --production=false && npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  }
}
